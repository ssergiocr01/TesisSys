@model IEnumerable<TesisSys.Models.Distritos>

<div class="panel panel-default">
    <div class="panel-heading">
        <strong>Lista de Distritos</strong>
    </div>

    @if (Model.Count() > 0)
    {
        <table class="table">
            <tr>
                <th>
                    Nombre
                </th>
                <th>
                    Estado
                </th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Nombre)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Estado)
                    </td>
                </tr>
            }
        </table>
    }
    else
    {
        @: <div class="text-muted">&nbsp;No tiene distritos registrados.</div>
    }
</div>

